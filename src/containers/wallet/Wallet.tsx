import React from "react";

interface Props {
  sum: number;
  currencySign: string;
}

const Wallet: React.FC<Props> = ({ sum, currencySign }) => {
  return <div>Nothing here</div>;
};

export default Wallet;
